#include "main.h"
/**
  * mul - multiplies two integers
  * @a: Variable to be multiplied
  * @b: Variable to be multiplied with
  * Return: product of a and b
  */

int mul(int a, int b)
{
	return (a * b);
}
