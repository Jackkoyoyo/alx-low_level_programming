#include "main.h"

/**
  * _isdigit - Function that check for digits
  * @c: Value to be checked
  * Return: 1 if digit else 0
  */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
