#include <stdio.h>

/**
  * main - entry point
  * Return: Always 0 (success)
  */

int main(void)
{
	int num = 0;

	for (num = 0; num <= 9; num++)
		printf("%d", num);

	printf("\n");
	return (0);
}
