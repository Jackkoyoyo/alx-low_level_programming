#include <stdio.h>

/**
  * main - entry point
  * Return: Always 0 (success)
  */

int main(void)
{
	unsigned int num = 48;

	for (num = 48; num <= 57; num++)
		putchar(num);

	putchar('\n');
	return (0);
}
